<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDisplayName</key>
	<string>OpenFlow Keyboard</string>
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.keyboard-service</string>
		<key>NSExtensionPrincipalClass</key>
		<string>$(PRODUCT_MODULE_NAME).KeyboardViewController</string>
		<key>NSExtensionAttributes</key>
		<dict>
			<key>IsASCIICapable</key>
			<false/>
			<key>PrefersRightToLeft</key>
			<false/>
			<key>PrimaryLanguage</key>
			<string>en-US</string>

			<!-- Why this is true, and it is the only reason:
			     iOS sandboxes a keyboard extension away from the App Group
			     container unless the user grants Allow Full Access. Without it
			     the keyboard cannot read `last.json` and has nothing to insert.
			     It buys OpenFlow nothing else: the keyboard makes no network
			     request (it cannot -- there is no networking code in this
			     target), keeps no keystrokes, and reads exactly one file that
			     this app wrote. The trade is explained in apps/ios/README.md and
			     is shown to the user before they turn it on. -->
			<key>RequestsOpenAccess</key>
			<true/>
		</dict>
	</dict>
</dict>
</plist>
